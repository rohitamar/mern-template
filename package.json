{
  "name": "MERN Template",
  "version": "1.0.0",
  "description": "Template to start a simple MERN + Redux project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install npm@latest && npm run build",
    "dev": "cd client && npm run build && cd ../ && node server.js"
  },
  "author": "Rohit Amarnath",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "mongoose": "^6.5.4",
    "path": "^0.12.7"
  }
}
